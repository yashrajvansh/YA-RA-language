# YA|RA rv0.2.0 — past the single action

The slide titled *The theory of everything (so far)* writes one number:

    Z = ∫ D(Fields) exp(i ∫ d⁴x √(-g) L)

That total has no observer. It cannot refuse. It is not an expression.

YA|RA does not replace R, F_μν, or ψ. It refuses to treat that integral as a door.
It computes a different number after deterministic shots:

    Z_YA|RA = Σ_k a_k [c_k passed]

`ya-ra measure --action` is the slide. The cut refuses it.
An unsigned Intent | Pattern is still a sentence. Missing provenance is recorded.
Born probability stays observational. Z is the attributed sum.

Canonical checks: words, exists, contains, eq, run (--allow-run), use.
See CONFORMANCE.md.
